name: web_pengaduan_admin
description: "Panel Admin Web Pengaduan Sekolah"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase & Layanan Pendukung
  firebase_core: 2.15.0
  cloud_firestore: 4.8.0
  firebase_auth: 4.7.0
  image_picker: 1.0.4
  firebase_storage: 11.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
