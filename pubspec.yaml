name: web_pengaduan_admin
description: "Panel Admin Web Pengaduan Sekolah"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Versi dikunci tanpa tanda ^ agar aman dari error PromiseJsImpl pada Flutter Web
  firebase_core: 2.15.0
  cloud_firestore: 4.8.0
  firebase_auth: 4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true